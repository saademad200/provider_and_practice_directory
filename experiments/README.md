# Experiments

Historical research runners are intentionally excluded from the judge-facing repository. The maintained reproducible path is:

```bash
python3 scripts/verify_pipeline.py --package submissions/latest_final_package.zip
```

The curated submission lives in `submissions/healthlynked_option_c_clean/`.
